version: "3.9"

services:
  backend:
    build:
      context: ./app
    ports:
      - "5000:5000"
    container_name: flask-backend

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:8080"
    container_name: vue-frontend